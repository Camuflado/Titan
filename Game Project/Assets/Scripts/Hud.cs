using UnityEngine;
using System.Collections;

public class Hud : MonoBehaviour
{
    /*
    public LivingEntity _LivingEntity;
    public GameUI _GameUI;
    public static int health { get; private set; }

    
    void Start()
    {


        BossOgre.OnHitStatic += OnHit;
        FindObjectOfType<Player>().OnDeath += OnPlayerDeath;
    }

    void OnHit()
    {
        /*if (_LivingEntity.health == 5)
            _GameUI.healthSprite.fillAmount = (float)1;
        else if (_LivingEntity.health == 4)
            _GameUI.healthSprite.fillAmount = (float)0.797;
        else if (_LivingEntity.health == 3)
            _GameUI.healthSprite.fillAmount = (float)0.601;
        else if (_LivingEntity.health == 2)
            _GameUI.healthSprite.fillAmount = (float)0.399;
        else if (_LivingEntity.health == 1)
            _GameUI.healthSprite.fillAmount = (float)0.197;
        else
            _GameUI.healthSprite.fillAmount = (float)0;
        health -= 1;
    }

    void OnPlayerDeath()
    {
        BossOgre.OnHitStatic += OnHit;
    }
*/

}
